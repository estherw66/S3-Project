stages:
- build
- test 

build:
  stage: build
  script:
    - cd S3-ITProject
    - ./gradlew build -x test
    - ./gradlew.bat --stop 
test:
  stage: test
  script:
    - ./gradlew test
    - ./gradlew.bat --stop
